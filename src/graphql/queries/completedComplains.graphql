query CompletedComplains($limit: Int!, $cursor: String) {
  completedComplains(limit: $limit, cursor: $cursor) {
    complains {
      id
      descriptionSnippet
      title
      createdAt
      latitude
      longitude
      wardNo
      latitude
      imagePublicId
      category
      createdAt
      user {
        user {
          id
          username
        }
      }
    }
    hasMore
  }
}
