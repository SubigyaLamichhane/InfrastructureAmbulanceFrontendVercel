query CompletedComplain($id: Int!) {
  completedComplain(id: $id) {
    title
    description
    latitude
    longitude
    createdAt
    category
    wardNo
    imagePublicId
    user {
      user {
        id
        username
        firstname
        lastname
        phonenumber
        email
      }
    }
  }
}
